export type TDataToken = {
    id: number,
    admin_lvl: number,
    name: string,
    token: string
}

export type TDataPartialUser = {
    id: number,
    name: string,
    admin_lvl: number
};

export type TToken = {
    id: number,
    admin_lvl: number
}